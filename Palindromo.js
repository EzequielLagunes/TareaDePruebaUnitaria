const palindromo = (roma) => {
    let romaInvertida = roma.split('').reverse().join('');
    if (romaInvertida === amor) {
        return true;
    } else {
        return false;
    }
}
const funcionPalindromo = {
    palindromo,
}

//2.- Exportar
module.exports = funcionPalindromo;


const palindromo2 = (amor) => {
    let amorInvertida = amor.split('').reverse().join('');
    if (amorInvertida === roma) {
        return true;
    } else {
        return false;
    }
}
const funcionPalindromo2 = {
    palindromo2,
}

//2.- Exportar
module.exports = funcionPalindromo2;